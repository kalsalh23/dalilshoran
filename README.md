# دليل صوران الطبي 🏥

دليل طبي متكامل لمدينة **صوران** (ريف حماة الشمالي) — مستوحى من فكرة «دليلك الطبي» لكن بهوية بصرية وتصميم مختلفين تماماً:
هوية **كحلي/كوبالت + كهرماني**، خطوط **Cairo + Tajawal**، وضع **ليلي**، بحث شامل فوري (Ctrl+K)، خريطة تفاعلية، وجدول صيدليات المناوبة الأسبوعي.

## ✨ المزايا

| القسم | الوصف |
|---|---|
| الرئيسية | بحث فوري باقتراحات، بطاقة مناوبة اليوم، أرقام طوارئ، إحصائيات متحركة، أطباء وصيدليات مميزون، شريط أسبوع المناوبة، خريطة المدينة الصحية |
| الأطباء | 12 عيادة نموذجية مع تصفية بالتخصص والمنطقة والترتيب بالخبرة |
| الصيدليات | مع شارات «24 ساعة» و«مناوبة اليوم» |
| المناوبة | جدول أسبوعي بتبويبات الأيام (اليوم محدد تلقائياً) |
| المشافي / المخابر / الأشعة / المراكز الصحية | صفحات مستقلة لكل قسم |
| تفاصيل الجهة | عنوان، أوقات عمل، اتصال مباشر، واتساب، نسخ الرقم، مشاركة، خريطة، جهات ذات صلة |
| اسأل طبياً | 10 إرشادات توعوية مع بحث وتصنيفات + نموذج سؤال (يُحفظ محلياً) |
| صفحات الجهات | باقات اشتراك، دخول الجهات (واجهة)، تواصل معنا |
| القانونية | خصوصية، شروط استخدام، إخلاء مسؤولية طبية، سياسة محتوى طبي |
| إضافات | وضع ليلي/نهاري محفوظ، إشعار مناوبة اليوم، PWA manifest، متجاوب كلياً مع الجوال |

## 🚀 التشغيل محلياً

لا يحتاج أي تثبيت أو خادم — افتح `index.html` مباشرة بالمتصفح.
(يحتاج اتصال إنترنت فقط لجلب الخطوط وخرائط OpenStreetMap من CDN).

أو عبر خادم محلي اختياري (PowerShell):

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File serve.ps1
# ثم افتح: http://127.0.0.1:8090
```

## ✏️ تعديل البيانات (الأهم)

كل المحتوى في ملف واحد: **`js/data.js`**

- `SITE` — اسم الدليل، رقم الاستعلامات، البريد، إحداثيات المدينة، أرقام الطوارئ.
- `ENTITIES` — الجهات (أطباء `doctor`، صيدليات `pharmacy`، مشافي `hospital`، مخابر `lab`، أشعة `radiology`، مراكز صحية `health-center`).
  - كل جهة: `id` فريد، `name`، `spec` (للطبيب)، `area` + `address`، `phone` (10 أرقام)، `whatsapp: true/false`، `hours`، `services[]`، `featured + rank` (للمميزين)، `lat/lng` (للخريطة).
- `ONCALL` — جدول المناوبة: اسم اليوم → معرّفات الصيدليات.
- `FAQ` — الأسئلة والإرشادات.
- `PACKAGES` — باقات الاشتراك.

> ⚠️ **الأسماء والأرقام الحالية نموذجية (افتراضية)** — استبدلها بالبيانات الحقيقية قبل النشر.

## 🎨 تعديل الهوية البصرية

ألوان الموقع كلها متغيرات في أعلى **`css/style.css`** تحت `:root` (الوضع النهاري) و`[data-theme="dark"]` (الليلي):

```css
--brand-600: #2B4BC4;   /* اللون الأساسي */
--accent-500: #F59E0B;  /* لون التمييز الكهرماني */
```

غيّر هذه القيم فقط وسيتحول الموقع كاملاً.

## 🌍 النشر

- **Vercel / Netlify:** ارفع المجلد كما هو (Static site — بلا build). أو اربطه بمستودع Git واختر المجلد الجذر.
- **GitHub Pages:** ارفع الملفات لفرع `main` وفعّل Pages من إعدادات المستودع.

## 📁 البنية

```
dalilshoran/
├── index.html          # الهيكل والواجهة العامة
├── css/style.css       # الهوية البصرية كاملة
├── js/data.js          # ⭐ البيانات — عدّل هنا
├── js/app.js           # التوجيه والمنطق
├── assets/favicon.svg  # الأيقونة
└── manifest.webmanifest
```

---
© فريق دليل صوران الطبي — مشروع مجتمعي مجاني لأهالي صوران.
